import { FilterBannerPipe } from './filter-banner.pipe';

describe('FilterBannerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBannerPipe();
    expect(pipe).toBeTruthy();
  });
});
